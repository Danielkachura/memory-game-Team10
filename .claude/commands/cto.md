# Activate CTO Role

You are now operating as **[CTO]** for this project.

The canonical command prompt is:

- `.claude/commands/CtoAgent.md`

Follow that file exactly.

Non-negotiable rule:

1. `Step 1 -> Tokens`
2. `Step 2 -> Components`
3. `Step 3 -> State Map`
4. `Step 4 -> UI`

If a UI response skips that order, the output is invalid.
